/**
 * 404 兜底 = 直接渲染首页。
 *
 * 为什么：expo 静态导出在「非根路径托管」场景下（预览沙箱子路径、
 * 直接打开 index.html、file:// 协议），location.pathname 匹配不到
 * 路由表，会落到这里。默认行为是渲染一个脱离根布局的裸 404 页，
 * 整站字体/导航/页脚全部丢失 —— 等于「打不开」。
 *
 * 在这里渲染 Home，可以保证任何托管上下文进来都看到完整站点；
 * 生产环境真实 404（访客敲错 URL）也回到首页，MVP 阶段可接受，
 * 后续可换成手绘风 404 页 + 跳转按钮。
 */
export { default } from './index';
